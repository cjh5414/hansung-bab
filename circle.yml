test:
    override:
        - pytest test_sample.py

dependencies:
    pre:
       -  pip install -r requirements.txt

deployment:
    production:
        branch: master
        heroku:
            appname: hansung-bab
